function output = sumVar(calcParam, inputParam)
%sumVar computes Sum Variance
%Notation described in 'Feature Definition and Algorithm.docx'
n = length(calcParam.sumVec);
SE=sumEntropy(calcParam, inputParam);
output = 0;
for i = 1:n
    output = output + ((i+1) - SE)^2*calcParam.sumVec(i);
end
end
