function output = secDiagM(calcParam, inputParam) %#ok<INUSD>
%secDiagM computes Second Diagonal Moment
%Notation described in 'Feature Definition and Algorithm.docx'
n = size(calcParam.comat, 1);
output = 0;
for i = 1:n
    for j = 1:n
        output = output + 0.5 * abs(i-j) * calcParam.comat(i, j);
    end
end
end