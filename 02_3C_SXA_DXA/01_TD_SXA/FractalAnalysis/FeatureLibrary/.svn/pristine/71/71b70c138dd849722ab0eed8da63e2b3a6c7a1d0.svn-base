function output = maxIntensProb(calcParam, inputParam) %#ok<INUSD>
%maxIntensProb computes Maximum of Intensity Probability
%Notation described in 'Feature Definition and Algorithm.docx'
Cx = sum(calcParam.comat, 2);
output = max(Cx);
end