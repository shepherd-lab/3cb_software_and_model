function output = sumAvg(calcParam, inputParam) %#ok<INUSD>
%sumAvg computes Sum Average
%Notation described in 'Feature Definition and Algorithm.docx'
n = length(calcParam.sumVec);
output = 0;
for i = 1:n
    output = output + (i+1)*calcParam.sumVec(i);
end
end