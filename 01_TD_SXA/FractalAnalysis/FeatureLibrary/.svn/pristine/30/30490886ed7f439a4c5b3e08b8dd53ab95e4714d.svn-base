function maskObj=maskCircle(imageObj, center,radius,NOP,style,draw)
%JW
THETA=linspace(0,2*pi,NOP);
RHO=ones(1,NOP)*radius;
[X,Y] = pol2cart(THETA,RHO);
X=X+center(1);
Y=Y+center(2);
maskObject=zeros(size(imageObj));

% k=1;
% while k<=1000
%     y1=round(X(k));
%     x1=round(Y(k));
%     a(x1,y1)=1;
%     k=k+1;
% end

% H=plot(X,Y,style);
% axis square;
H=roipoly(a,X,Y);

if draw=='y'
    ROI.BiopsyROIx=X;
    ROI.BiopsyROIy=Y;
%     plot(X,Y,style);
end